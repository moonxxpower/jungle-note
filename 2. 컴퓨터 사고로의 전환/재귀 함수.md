# 재귀 함수

🔎 *참고 자료* <br>
    - [📚 Do it! 자료구조와 함께 배우는 알고리즘 입문] **5장** 재귀 알고리즘 <br>
    - [함수 호출의 원리 및 재귀호출](http://10bun.tv/beginner/episode-4/)
    

🔑 **키워드**

`재귀`

자기 자신을 포함하고 다시 자기 자신을 사용하여 정의되는 경우를 **재귀** (Recursion) 라고 한다. 재귀를 효과적으로 사용하면, 이러한 정의뿐만 아니라 프로그램을 간결하고 효율성 좋게 작성할 수 있다. 하지만, 메모리 성능 면에서 보다 떨어진다.

함수 내부에서 함수가 자기 자신을 또 다시 호출하는 행위를 **재귀 호출** (Recursion Call) 이라고 부른다.

- **함수 호출의 원리**

(1) **일반적인 함수 호출 및 리턴 과정**

![1](https://github.com/moonxxpower/jungle-note/assets/118599217/4acd62a1-6654-4e9d-bae6-6cef8b1ad5ce)

(2) **재귀 함수의 호출 및 리턴 과정**

![2](https://github.com/moonxxpower/jungle-note/assets/118599217/dbf1b96e-c246-47fe-9c9d-e89713e47eed)

모든 함수는 호출되면, 메모리에 새로운 공간을 확보해서 매번 전혀 다른 공간에서 작업이 진행된다. 소스 코드에서는 같은 공간처럼 보이지만, 실제 실행되는 코드는 전혀 다른 공간에서 이루어진다. (호출할 때마다 새로운 객체가 생성된다.)

main() 함수에서 처음 재귀함수를 실행했을 때, “재귀함수 1”이라는 메모리 공간이 생겨서 작업을 진행하다가 다시 재귀함수를 실행한다면 새로 “재귀함수 2”이라는 메모리 공간이 생성되는 방식이다. 

이런 방식을 반복하다보면, 같은 코드가 메모리 공간만 옮겨다니면서 무한히 반복되기 때문에 메모리가 부족할 때까지 멈추지 않고, 반복하다가 프로그램이 종료된다. 이러한 부분 때문에 재귀함수를 작성할 때에는 언제 멈춰야 할지 **탈출조건**이 필요하다.

위의 그림에서 “재귀함수 3”은 탈출조건을 만나서 실행이 멈추게 되고, 이후 이전 함수로 복귀하여 나머지 코드를 진행하고, 다시 이전 함수로 복귀하는 과정이 계속 진행된다.